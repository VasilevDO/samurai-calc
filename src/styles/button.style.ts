const primary = `
    color:white;
    background-color: inherit;
    
    &:hover {
        background-color:rgba(255,255,255,0.12);
    }
`;

const secondary = `
    color: #2B589A;
    background-color: #FFFFFF;

    &:hover {
        background-color:rgba(255,255,255,0.7);
    }
`;

const buttonStyle = {primary, secondary};

export default buttonStyle;
